.sidebar
  .header
    .header__name
      = current_user.name
    .header__lists
      .list
        = link_to '#' do
          = icon('fa', 'edit',class: 'icon')
      .list
        = link_to edit_user_path(current_user) do
          = icon('fa', 'cog',class: 'icon')

  .groups
    .group
      .group__name
        グループ名
      .group__message
        tesuto